# データベース設定
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=event_store
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# アプリケーション設定
MIX_ENV=dev
PORT=4000
PHX_HOST=localhost

# 秘密鍵（本番環境では必ず変更してください）
SECRET_KEY_BASE=dev-secret-key-base-must-be-at-least-64-characters-long-replace-in-production
ENCRYPTION_KEY=dev-encryption-key-32-characters

# gRPC サービス設定
COMMAND_SERVICE_HOST=localhost
COMMAND_SERVICE_PORT=50051
QUERY_SERVICE_HOST=localhost
QUERY_SERVICE_PORT=50052

# 監視設定
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
OTEL_SERVICE_NAME=elixir-cqrs
OTEL_RESOURCE_ATTRIBUTES=deployment.environment=development

# プール設定
POOL_SIZE=10